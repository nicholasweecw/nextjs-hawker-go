// Without a defined matcher, this one line will be applied to all routes
// export { default } from "next-auth/middleware";

// Applies next-auth only to matching routes
// export const config = {
//   matcher: "/favourites",
// };
